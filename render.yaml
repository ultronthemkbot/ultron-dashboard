services:
  - type: web
    name: ultron-dashboard
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DASHBOARD_PASSWORD
        value: ULTRON12345
      - key: JWT_SECRET
        value: ultron-dashboard-secret-key-2024
