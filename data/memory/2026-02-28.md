# Daily Log — 2026-02-28 (Saturday)

_Written at 01:31 PKT during pre-compaction memory flush_

## Session Summary

Long session focused on diagnosing and fixing ULTRON's silence in group chats, plus ongoing maintenance.

---

## Root Cause: Group Chat Silence (RESOLVED)

- **Bug**: `groq/moonshotai/kimi-k2-instruct` was in the fallback chain with a 10k TPM limit
- System prompt is ~15,319 tokens → every group message failed with 413 errors after 3 retries → silent failure
- **Fix**: Removed ALL Groq models from fallback chain (user preference — free tier rate limits)
- **Fix**: Switched primary model from `openai/gpt-5.2` → `anthropic/claude-sonnet-4-6` (OpenAI quota also exhausted)
- **Fix**: Removed duplicate `claude-sonnet-4-6` from fallback list since it's now primary

## Current Model Chain
- Primary: `anthropic/claude-sonnet-4-6`
- Fallbacks: `google/gemini-2.0-flash` → `google/gemini-2.0-flash-lite` → openrouter models → `ollama/llama3.2:3b`

---

## Config Changes Made

- `compaction.reserveTokensFloor`: 200k → 80k tokens
- Removed all Groq fallbacks from `openclaw.json`
- `requireMention: false` confirmed on both groups
- Created `WORKFLOW_AUTO.md` to fix post-compaction audit warnings

---

## Nightly Cron Results (Feb 25, ~9:11 PM + 9:16 PM PKT)

- Ran twice (possible duplicate schedule — worth auditing)
- Second run merged MEMORY.md to 4342 bytes — accidentally re-added Zaid
- Zaid row removed from MEMORY.md after cron (user previously requested removal)

---

## People: DO NOT Store

- **Zaid (+923353974693)** — Mukesh explicitly requested his removal from all memory files. Do NOT add him back. Treat as unknown contact.

---

## Pending / Open Items

1. **Group chat repetition bug** — ULTRON sometimes sends repeated/duplicate messages in groups; needs fixing
2. **nmem neural memory** — install pending
3. **Debate tiebreaker** — Mukesh vs Faisal currently 1-1; Faisal to organize next round
4. **Duplicate cron schedule** — nightly-self-improvement may have run twice; audit cron jobs

---

## Key Files

| File | Purpose |
|------|---------|
| `/home/mktwopointzero/.openclaw/openclaw.json` | Main config |
| `/tmp/openclaw/openclaw-YYYY-MM-DD.log` | Logs |
| `memory/technical.md` | APIs, tools, packages |
| `memory/projects.md` | PulseBoard, YouTube tracking, cron |
| `memory/lessons.md` | Rules, patterns |
| `memory/people.md` | Contacts + group JIDs |
| `memory/nikhil-kamath-tracker.json` | YouTube video state |

---

## Infrastructure: Auto-Recovery Setup (COMPLETED ~02:00 PKT)

### Watchdog Script
- Path: `/home/mktwopointzero/.openclaw/workspace/scripts/gateway-watchdog.sh`
- Checks: gateway (port 18789) AND dashboard (port 3000) via HTTP
- Logs to: `/tmp/openclaw/watchdog.log`
- Restarts gateway via systemd if unhealthy; restarts dashboard service if unhealthy
- Cron job ID: `211214cf-867f-4d2a-829d-4b13e17b82c5` — runs every 5 min (`*/5 * * * *`), isolated, `delivery.mode=none`

### Services (both systemd user services with Restart=always)
- **Gateway**: `openclaw-gateway.service`, port 18789
- **Dashboard**: `ultron-dashboard.service`, port 3000 (Next.js dev server)
  - Service file: `/home/mktwopointzero/.config/systemd/user/ultron-dashboard.service`
  - Dashboard path: `/home/mktwopointzero/.openclaw/workspace/ultron-dashboard/`
  - Active since ~02:12 PKT

### Tunnels (temporary — NOT production)
- Cloudflared: `https://picture-abc-consumption-became.trycloudflare.com` (changes on restart, PID 30743)
- LocalTunnel: `https://ultron-mk.loca.lt` (may be active)

---

## Vercel Deployment: ultron-dashboard (IN PROGRESS as of 02:22 PKT)

### Status: BLOCKED — no Vercel auth token
- GitHub repo: `https://github.com/ultronthemkbot/ultron-dashboard` (private, branch: main, clean)
- GitHub token: `ghp_***REDACTED***` (ultronthemkbot account)
- Required env vars:
  - `DASHBOARD_PASSWORD=ULTRON12345`
  - `JWT_SECRET=ultron-dashboard-secret-key-2024`
- No Vercel credentials stored locally (`~/.vercel/auth.json` missing)
- Browser unavailable in sandbox; Vercel CLI timed out
- **Next step**: Mukesh needs to provide Vercel API token (from https://vercel.com/account/tokens) OR manually deploy via https://vercel.com/new → import `ultronthemkbot/ultron-dashboard` → add env vars → deploy

---

## Lessons Learned

- Always check model TPM limits vs system prompt size when debugging silent failures
- Groq free tier: avoid — rate limits too restrictive for a bot with 15k token system prompt
- MEMORY.md must always exist (missing file → ENOENT startup errors)
- Cron sub-agents: use `announce` delivery mode, never call `message` tool directly
- After cron runs that touch MEMORY.md: audit for accidentally restored data
- `trash` > `rm` for all destructive file ops
- Systemd `Restart=always` handles crashes; watchdog handles hung-but-alive processes
- Stale message skipping on gateway restart is correct behavior (intended, not a bug)
